# **woowa precourse - week 3: 로또**

## **1. 기능 요구 사항**

간단한 로또 발매기를 구현한다.

- 로또 번호의 숫자 범위는 1~45까지이다.
- 1개의 로또를 발행할 때 중복되지 않는 6개의 숫자를 뽑는다.
- 당첨 번호 추첨 시 중복되지 않는 숫자 6개와 보너스 번호 1개를 뽑는다.
- 당첨은 1등부터 5등까지 있다. 당첨 기준과 금액은 아래와 같다.
    - 1등: 6개 번호 일치 / 2,000,000,000원
    - 2등: 5개 번호 + 보너스 번호 일치 / 30,000,000원
    - 3등: 5개 번호 일치 / 1,500,000원
    - 4등: 4개 번호 일치 / 50,000원
    - 5등: 3개 번호 일치 / 5,000원
- 로또 구입 금액을 입력하면 구입 금액에 해당하는 만큼 로또를 발행해야 한다.
- 로또 1장의 가격은 1,000원이다.
- 당첨 번호와 보너스 번호를 입력받는다.
- 사용자가 구매한 로또 번호와 당첨 번호를 비교하여 당첨 내역 및 수익률을 출력하고 로또 게임을 종료한다.
- 사용자가 잘못된 값을 입력할 경우 `IllegalArgumentException`을 발생시키고, "[ERROR]"로 시작하는 에러 메시지를 출력 후 그 부분부터 입력을 다시 받는다.
    - `Exception`이 아닌 `IllegalArgumentException`, `IllegalStateException` 등과 같은 명확한 유형을 처리한다.

## **2. 기능 목록 리스트**

| **번호** | **기능명**     | **설명**                                         |
|--------|-------------|------------------------------------------------|
| F-01   | 금액 입력       | 구입 금액을 입력받는다                                   |
| F-02   | 당첨 번호 입력    | 당첨 번호 6개를 쉼표로 구분하여 입력받는다                       |
| F-03   | 보너스 번호 입력   | 보너스 번호 1개를 입력받는다                               |
| F-04   | 로또 번호 검증    | 번호가 1~45, 6개 중복 없음, 오름차순인지 검증                  |
| F-05   | 구입 금액 검증    | 1,000원 단위, 양수, 숫자 검증                           |
| F-06   | 보너스 번호 검증   | 1~45, 당첨 번호와 중복 불가 검증                          |
| F-07   | 로또 발행       | 금액 기반으로 N개 로또 번호 자동 생성                         |
| F-08   | 당첨 결과 계산    | 사용자 로또와 당첨 번호, 보너스 번호 비교 → 등수별 당첨 내역 반환        |
| F-09   | 수익률 계산      | 총 상금 및 수익률(%) 계산, 소수점 첫째자리 반올림                 |
| F-10   | 출력          | 구매 수량, 로또 번호 목록, 당첨 통계 결과, 수익률 등 출력            |
| F-11   | 예외 처리       | 잘못된 입력/파싱/범위/중복/가격에 대해 에러 메시지 출력, 해당 입력 단계로 복귀 |
| F-12   | 상수/메시지 분리   | 모든 하드 코딩(문자/숫자/에러 메시지 등) 상수 및 enum으로 분리        |
| F-13   | 검증/파서/유틸 분리 | 입력 파싱 및 검증 로직 별도 유틸 클래스로 분리                    |
| F-14   | 출력 포맷 분리    | 결과 출력 서식/포맷 빌더 별도 클래스로 분리                      |
| F-15   | 테스트 코드 작성   | 로직/유틸/예외 전반에 대한 테스트 작성                         |

## **3. 클래스 구조**

```
src/main/java/lotto/
├── Application.java               # 메인
├── LottoApplication.java          # 게임 flow
├── model/
│   ├── lotto/
│   │   ├── Lotto.java             # 단일 로또(번호 검증 포함)
│   │   ├── Lottos.java            # N개 로또 컬렉션, 평가
│   │   ├── PrizeLotto.java        # 당첨번호+보너스 저장/검증
│   │   └── Budget.java            # 금액/티켓 수 관리
│   └── lottoresult/
│   │   ├── LottoRank.java         # 등수/금액 분류(enum)
│   │   └── LottoResult.java       # 등수별 개수/총 합산
│   └── constant/
│       └── LottoRules.java        # 범위, 장수 등 상수
├── util/
│   ├── BudgetValidator.java       # 금액 입력 검증/파싱
│   └── LottoNumberParser.java     # 번호 입력 검증/파싱
├── exception/
│   ├── ErrorMessage.java          # 에러 메시지(enum)
│   └── LottoException.java        # 커스텀 예외
├── view/
│   ├── InputView.java             # 입력 뷰
│   ├── OutputView.java            # 출력 뷰
│   ├── builder/
│   │   └── OutputFormatBuilder.java # 출력 포맷 빌더
│   └── constant/
│       └── ViewMessages.java      # 출력 문자열 상수
```

## **5. 실행 예시**

```
구입금액을 입력해 주세요.
8000

8개를 구매했습니다.
[8, 21, 23, 41, 42, 43]
[3, 5, 11, 16, 32, 38]
[7, 11, 16, 35, 36, 44]
[1, 8, 11, 31, 41, 42]
[13, 14, 16, 38, 42, 45]
[7, 11, 30, 40, 42, 43]
[2, 13, 22, 32, 38, 45]
[1, 3, 5, 14, 22, 45]

당첨 번호를 입력해 주세요.
1,2,3,4,5,6

보너스 번호를 입력해 주세요.
7

당첨 통계
---
3개 일치 (5,000원) - 1개
4개 일치 (50,000원) - 0개
5개 일치 (1,500,000원) - 0개
5개 일치, 보너스 볼 일치 (30,000,000원) - 0개
6개 일치 (2,000,000,000원) - 0개
총 수익률은 62.5%입니다.
```

## **6. 예외 처리 명세 요약**

| **오류 상황**    | **예외**         | **메시지 예시**                          |
|--------------|----------------|-------------------------------------|
| 로또 번호 6개 아님  | LottoException | [ERROR] 로또 번호는 6개여야 합니다.            |
| 로또 번호 중복     | LottoException | [ERROR] 로또 번호는 중복될 수 없습니다.          |
| 번호 1~45 아님   | LottoException | [ERROR] 로또 번호는 1부터 45 사이의 숫자여야 합니다. |
| 금액 오입력       | LottoException | [ERROR] 구입 금액은 1,000원 단위의 양수여야 합니다. |
| 금액이 숫자 아님    | LottoException | [ERROR] 구입 금액은 숫자여야 합니다.            |
| 당첨과 보너스 중복   | LottoException | [ERROR] 보너스 번호는 당첨 번호와 중복될 수 없습니다.  |
| 보너스 1~45 아님  | LottoException | [ERROR] 보너스 번호는 1부터 45 사이여야 합니다.    |
| 번호/보너스 파싱 실패 | LottoException | [ERROR] 로또 번호는 쉼표(,)로 구분된 숫자여야 합니다. |

## **7. 테스트 구조**

```
src/test/java/lotto/
├── ApplicationTest.java             # 미션 기본 테스트
├── LottoTest.java                   # 미션 기본 테스트
├── model/
│   ├── lotto/
│   │   ├── LottoTest.java           # 단일 로또 검증
│   │   ├── LottosTest.java          # N개로또/평가
│   │   ├── BudgetTest.java          # 금액 티켓수/예외
│   │   └── PrizeLottoTest.java      # 당첨/보너스 예외포함
│   └── lottoresult/
│       ├── LottoRankTest.java       # 등수/금액 구분
│       └── LottoResultTest.java     # 등수별 집계
├── util/
│   ├── BudgetValidatorTest.java     # 금액 파서
│   └── LottoNumberParserTest.java   # 번호/보너스 파서
```